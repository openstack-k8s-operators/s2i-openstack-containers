acl SSL_ports port 443
acl Safe_ports port 80
acl Safe_ports port 443
acl Safe_ports port 1025-65535

http_access deny !Safe_ports
http_access deny CONNECT !SSL_ports
http_access allow all

http_port 3128
pid_filename none

collapsed_forwarding on
connect_retries 5
maximum_object_size 1024 MB
cache_mem 256 MB
cache_dir ufs /var/spool/squid/cache 20480 16 256

# Keep one consistent repository snapshot during a build invocation and ignore
# the cache-bypass header sent by DNF for repository metadata.
refresh_pattern -i /repodata/repomd\.xml$ 120 100% 120 override-expire ignore-reload
refresh_pattern . 0 20% 4320

# Rewrite CentOS Stream's plain-HTTP repository URLs to HTTPS before fetching
# them so Squid can cache the response without intercepting client TLS.
url_rewrite_program /etc/squid/centos-rewrite
url_rewrite_children 4 startup=1 idle=1 concurrency=0

coredump_dir /var/spool/squid
access_log stdio:/dev/stdout
cache_log /dev/stderr
