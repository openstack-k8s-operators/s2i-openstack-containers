# Additional Python packages not in neutron's requirements.txt
# Database driver (mysql extra for oslo.db)
oslo.db[mysql]
# Caching backend (dogpile extra for oslo.cache)
oslo.cache[dogpile]
# Optional API extension service plugins loaded by neutron-server
# (neutron-fwaas and tap-as-a-service are built from source alongside
# neutron, see sources.txt — the pypi/upper-constraints releases are
# incompatible with master neutron)

# Paste/PasteDeploy require pkg_resources, removed in setuptools 82
setuptools==80.10.2

# HTTP tracing exporter extra for oslo.messaging (OpenTelemetry over HTTP)
oslo.messaging[tracing-http]
