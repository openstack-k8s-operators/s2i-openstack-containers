# All plugins are built from source via sources.txt and installed as wheels.
# OpenStack CLI used by run_tempest.sh (image/flavor management, cleanup)
python-openstackclient
# Provides subunit2html used by run_tempest.sh to generate HTML test reports
os-testr
# Provides the junitxml module imported by subunit2junitxml (used by
# run_tempest.sh to generate JUnit XML reports); python-subunit does not
# pull this in automatically.
junitxml
# telemetry-tempest-plugin deps
gabbi==4.1.1
